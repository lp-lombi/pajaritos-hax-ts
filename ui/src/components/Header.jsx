import React from "react";
import "./Header.css";

export const Header = () => {
    return (
        <header>
            <span>PAJARITOS HOST</span>
        </header>
    );
};

export default Header;
